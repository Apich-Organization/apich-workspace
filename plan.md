目前你工作目录下的项目写的不行，存在很多设计上的缺陷和代码质量问题。我们需要的是：
1. login界面支持passkey与password两种模式；默认session token过期自动跳转过去
2. sign up界面允许新注册用户（平台管理员可设置邮件或邀请码模式），可选注册用户后注册org
3. dashborard首页就是一个project展示列表，做侧边栏边缘栏目，比如admin会有admin panel：但各个层级的admin的admin panel和平台admin是要分开的；同时都有个人settings。也就是，平台admin三个settings org／team admin两个 个人一个
4. 有一个demo project，有全部功能的demo文档。cargo-slide项目详见~/dev/cargo-slide。你需要了解。
5. 仅有每个层级的admin可以创建subteam和邀请别的admin
6. 每个project从主页点击进去后，有vcs记录与管理、共享权限管理（公开链接／对特定账号的邀请；read only／read and review／read write and review）和最主要的：文件列表。
7. 点击打开一个文件后，根据文件类型进入对应的app：比如typst／latex进入文档或幻灯片工具，左面file outline和代码右面是预览；表格模式左边是完整的表格显示，可以运行表格操作命令（类似office）和更原始的sql数据库操作，先是表格他才是数据库，而不是让你把数据库直接列在那里，我说的是表格不是数据库；关于note，同样类似：请给这个统一note设计一个统一文件格式，并实现那几种功能的功能面板。你现在在严重偷懒必须重写。
8. 整体前端画风要细腻且高级感，不要采取古板的纯蓝白配色，但也不要搞什么“极客风”，而应该是灵动动态渐变的蓝白，采取毛玻璃 贝塞尔曲线和动感呼吸。以及 文案不要写任何宣传语，只写这是什么功能不要写背后技术多厉害。除此之外，容器应该是自动化启动／shutdown的，不要把它当作一个需要用户自己开启的东西。
原本一个参考版本的计划是这样：
### 1. 学术排版双引擎（Typst & LaTeX）
- **Typst 原生支持**：提供亚毫秒级的文档编译能力，完美支持复杂的数学公式、矢量图、交叉引用与现代学术排版。
- **LaTeX 兼容层**：完整保留传统 LaTeX 编译链，确保与现有学术生态无缝对接，支持一键导出高精度 PDF。

### 2. 王牌幻灯片系统 (`cargo-slide`)
- **深度集成**：内置基于 `cargo-slide` 的幻灯片引擎，支持极客风格的领域特定语言（DSL）。
- **富媒体与交互**：原生支持内联音视频（如环境音轨、背景音乐）、13种硬件加速页面转场、激光笔轨迹、多色彩白板涂鸦，以及内联 CSV/SQLite 图表动态渲染。

### 3. 数据库驱动的结构化表格 (`SQLite Tables`)
- **表格即数据库**：打破传统电子表格的封闭形态，每一个表格本质上就是一个打包好的 **SQLite 数据库文件 (`.db/.sqlite`)**。
- **双模语法驱动**：
  - **高阶业务语法**：提供类似 Notion/Airtable 的直观操作与快捷公式。
  - **原生 SQL 语法**：完全开放 SQLite 语法支持，用户可直接编写复杂 SQL 查询、多表关联、数据清洗与视图聚合。

### 4. 全能知识图谱与统一知识库
- **一体化空间**：无缝聚合个人笔记（Markdown）、双链 Wiki、白板（Whiteboard）、日历日程与任务看板（Kanban）。
- **视图联动**：任务和日程直接与 Markdown 文档中的元数据双向绑定，实现“写文档即建任务，看日程即见进度”。

### 5. 多语言高性能计算沙盒
- **多语言支持**：内置对 **Python、R 以及 Rust** 的计算支持。
- **Notebook 驱动**：代码块与文本混排，用户可直接在工作空间内运行科学计算脚本、处理数据并实时回传图表结果。

### 7. 解耦的外部生态挂载
- **轻量化导航**：不重复造轮子。针对即时通讯、音视频会议、企业网盘及各类 AI 辅助软件，Workspace 采用解耦设计，仅通过统一的导航仪表盘（Dashboard）供管理员配置外部系统入口。
原本的一个定位规划：
定位：目前的主要市场上，有overleaf、google workspace、O365等专用型或复合型workspace解决方案，属于红海竞争；而在云编程领域，github workspace等也构成了同质化竞争。但是，目前却大量项目忽视了一个实际存在的痛点，学术与技术社区的编码与文档是高度耦合的数据驱动的，且需要严格的版本控制，且与代码开发不同，文档工作是需要高度协作化和便捷化的。
基础架构：
主要分为容器矩阵、后端服务器、前端UI三个部分
容器矩阵为核心，遵循着一个user的一个project（可能是共享的，但这里是说working copy）对应一个容器。预装typst LaTeX cargo-slide r Python git和rust等工具，以及我们的apich-vcs的cli工具；同时，我们注意到传统文档ai解决方案不足以解决数据和代码驱动型文档开发，故直接在容器里集成opencode aider goose claude code codex google antigravity cli等agent工具，用户可以直接让agent在容器内对数据做分析和文档编写。后端服务器，基本概念是project，并且要构建严格的通用多层递归team权限模型支持。后端应当提供对project的元信息存储，以及merge等操作的无指针实现。前端，以project为核心，dashboard围绕project展开，每个project下就是文件目录、配置和vcs管理。点开任何一个文件就进入统一面板：对于文档类工作，左侧typst/LaTeX源代码和file outline和vcs，右侧是预览；对幻灯片类工作，左侧是typst源代码和vcs右侧是预览；对于表格类工作，左侧是表格和vcs，右侧是可以展开的编程与agent窗口；对一般代码文件也是一样，实现一个简洁版本带基础高亮的web端编辑器，右侧是可以展开的编程与agent窗口；对综合笔记、wiki、calendar、文献管理、白板等，采取类似布局。
所有操作严格运行在对应的容器中，相当于将github workspace的文档用版本；但我们不追求成为另一个github workspace，只是会提供git兼容，因为对于学术和技术社区来讲，会更习惯在本地跑数据和写代码，再加上云端沙箱算力也不够。
核心技术壁垒的话，其实不算太多，这个架构本身只能算一个体验壁垒。但真正的壁垒的话，是cargo-slide本身的复杂度，作为一个代码生成元编程引擎和完整框架，是主要技术壁垒，以及我们已经开发完毕的自研vcs系统（也兼容git 所以有兜底和兼容性）也是真正的技术壁垒。以及我们采用全栈rust和podman，本身有一定安全性和性能优势。
不要强制大家换IDE，我自己也讨厌vscode，也不要试图说服大家离开console，我们只做文档工作支持不做云端IDE，容器化也只是一个实现手段而非以云端沙箱为核心卖点。而且我们也不做git托管平台---不搞重型cicd兼容。
ai问题的话，那个是可支持，但不代表是核心亮点也不需要过度宣传。用户如果要用的话可以bring your own subscription或api key，我们不是做ai的。
我们可以做统一workspace入口，也可以提供SSO接口，也就是允许org的admin配置dashboard，上面连接到比如chat平台 会议平台 云盘等的链接：但我们自己不会去做这些，因为这些太红海了且会导致很臃肿。
目前，apich-container apich-db apich-vcs有一些测试，只是需要一些完善，但apich-web需要全线修理；cargo-slide已完成，在~/cargo-slide，不需要修改只需要调用，当然有bug的话可以改。cargo-slide已发布在crates.io。完整的这段prompt我会放在plan.md供你遗忘后查阅。
前端ui设计一定注意和正式性，不要到处堆满技术宣传和emoji；以及，技术要无感才是好技术－－－而不是手动管理一切比如沙箱只为看起来的专业实则不好用。开subagent要克制，不要把任务分的太细－－－那样容易分成碎块效率更低，还容易context不完整。
